# Cart block

Render each line using its stable line ID, not SKU alone. Keep quantity/removal accessible. The cart and API order projection live in `scripts/commerce/cart.js`; see `docs/commerce/architecture.md` and cart tests.
