# Completion block

Never show success from cached order data, URL parameters, or approval alone. Retrieve the matching order from the Commerce API with tab-scoped email proof and require payment_completed. On lookup failure show a retry state, not thanks. See review flow tests.
